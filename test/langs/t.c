#include <stdlib.h>
#include <stdio.h>
/* #include <string.h> */

int main (int argc, const char* argv[]) /* hello world */
{
  printf("# of args: %d\n", /* hello world */ argc);
  /* hello world */

  for (int i = 0; i < argc; i++) {
    printf("arg #%d: %s\n", i, argv[i]);
  }

  return EXIT_SUCCESS;
/* hello world */}
